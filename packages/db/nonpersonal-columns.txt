analytics_event.id
analytics_event.tenant_id
analytics_event.name
analytics_event.at
analytics_event.props
audit_log.id
audit_log.tenant_id
audit_log.at
audit_log.ip
audit_log.action
audit_log.resource_type
audit_log.resource_id
audit_log.sensitive_read
employment.id
employment.tenant_id
employment.created_at
employment.employee_number
employment.hire_date
employment.exit_date
employment_version.id
employment_version.tenant_id
employment_version.employment_id
employment_version.recorded_at
employment_version.superseded_at
employment_version.org_unit_id
employment_version.job_title
employment_version.cost_centre
employment_version.idempotency_key
job_position.id
job_position.tenant_id
job_position.code
job_position.created_at
job_position_version.id
job_position_version.tenant_id
job_position_version.position_id
job_position_version.org_unit_id
job_position_version.headcount
job_position_version.status
job_position_version.valid_from
job_position_version.valid_to
job_position_version.recorded_at
job_position_version.superseded_at
legal_hold.id
legal_hold.tenant_id
legal_hold.person_id
legal_hold.placed_at
legal_hold.released_at
org_unit.id
org_unit.tenant_id
org_unit.code
org_unit.created_at
org_unit_version.id
org_unit_version.tenant_id
org_unit_version.org_unit_id
org_unit_version.valid_from
org_unit_version.valid_to
org_unit_version.recorded_at
org_unit_version.superseded_at
person.id
person.tenant_id
person.created_at
person.updated_at
transparency_ledger.id
transparency_ledger.tenant_id
transparency_ledger.subject_employment_id
transparency_ledger.decided_at
transparency_ledger.effective_from
transparency_ledger.ai_involved
employment_version.valid_from
employment_version.valid_to
tenant_record_view_setting.id
tenant_record_view_setting.tenant_id
tenant_record_view_setting.changed_at
identity_link.id
identity_link.tenant_id
identity_link.created_at
